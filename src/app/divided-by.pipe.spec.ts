import { DividedByPipe } from './divided-by.pipe';

describe('DividedByPipe', () => {
  it('create an instance', () => {
    const pipe = new DividedByPipe();
    expect(pipe).toBeTruthy();
  });
});
